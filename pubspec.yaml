name: shopster
description: "Shopster app"
publish_to: 'none'

version: 0.0.1+1

environment:
  sdk: ^3.6.0

dependencies:
  flex_color_scheme: ^8.0.2
  flutter:
    sdk: flutter
  get: ^4.6.6
  iconsax: ^0.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
